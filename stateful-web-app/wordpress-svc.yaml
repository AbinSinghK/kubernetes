apiVersion: v1
kind: Service
metadata:
  name: wordpress-service
  namespace: wordpress-demo
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80  
  selector:
    app: wordpress